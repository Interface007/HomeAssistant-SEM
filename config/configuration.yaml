
# Loads default set of integrations. Do not remove.
default_config:

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes

automation: !include automations.yaml

# Dateibasierte Automationen, thematisch gruppiert (im UI sichtbar, aber nur
# per Datei editierbar; Reload: Entwicklerwerkzeuge -> YAML -> Automationen)
automation manual: !include_dir_merge_list automations/
script: !include scripts.yaml
scene: !include scenes.yaml

# Alle Template-Sensoren liegen als Dateien unter templates/
# (outdoor.yaml, rooms.yaml, epaper.yaml)
template: !include_dir_merge_list templates/

# Klassische Plattform-Sensoren (statistics für 24-h-Mittelwerte)
sensor: !include sensors.yaml

rest_command: !include rest_commands.yaml

# YAML-Dashboard (Datei: /config/dashboards/matzen.yaml)
lovelace:
  mode: storage
  dashboards:
    matzen-home:
      mode: yaml
      title: Matzen
      icon: mdi:home-analytics
      show_in_sidebar: true
      filename: dashboards/matzen.yaml

input_boolean:
  trigger_forecast_update:
    name: Trigger Forecast Update
    icon: mdi:update

# Verbrauchszähler (Quelle: AI-on-the-Edge watermeter, Shelly 3EM)
utility_meter:
  water_daily:
    source: sensor.watermeter_value
    cycle: daily
  water_monthly:
    source: sensor.watermeter_value
    cycle: monthly
  power_main_daily:
    source: sensor.power_main_energy
    cycle: daily
